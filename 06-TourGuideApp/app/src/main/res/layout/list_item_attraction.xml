<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list_item_attraction_relative_layout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_item_attraction_size"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/attraction_image_image_view"
        android:layout_width="@dimen/list_item_attraction_size"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        tools:src="@mipmap/ic_launcher" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="@dimen/margin_half"
        android:layout_marginRight="@dimen/margin_double"
        android:layout_toRightOf="@id/attraction_image_image_view"
        android:orientation="vertical">

        <TextView
            android:id="@+id/attraction_name_text_view"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="bottom"
            android:textColor="@color/colorPrimaryText"
            android:textSize="@dimen/text_size_title"
            android:textStyle="bold"
            tools:text="Attraction Name" />

        <TextView
            android:id="@+id/attraction_description_text_view"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="top"
            android:textColor="@color/colorSecondaryText"
            android:textSize="@dimen/text_size_subheading"
            tools:text="Attraction description." />
    </LinearLayout>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="@dimen/margin_half"
        android:src="@drawable/ic_directions_black" />
</RelativeLayout>
